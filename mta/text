1、将需求写入Excel模版，读取Excel数据（data1）
2、接入gaia平台，按照需求定制自动化用例
3、执行自动化用例，读gaia接口数据，获取学生ID和起始时间
4、拿stu_id、start_time、end_time读神策接口，获取event_id和event_params值（data2）
5、data1和data2数据对比处理（分为事件处理和属性处理，将事件未上报的查出来；然后在已经上报的数据里面对比属性字段，将漏报、多报的属性查出来）
6、数据对比接入pytest框架，将对比结果集成至allure展示，目前已接入Jenkins，可持续集成
7、后续将整体功能迁入平台，指定用例回归。